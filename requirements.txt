# Runtime dependencies. Lower bounds only (no upper caps) so redfish_ctl installs
# cleanly ALONGSIDE other stacks — it is a CLI others embed, not a pinned app.
# setuptools is intentionally NOT here: it is a build tool, not imported at runtime.
requests>=2.28
tqdm>=4.61
urllib3>=1.26
pygments>=2.14
PyYAML>=6.0
