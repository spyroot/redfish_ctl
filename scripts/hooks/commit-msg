#!/usr/bin/env bash
# commit-msg hook: reject an agent identity in the message. Enable once per clone:
#   git config core.hooksPath scripts/hooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
exec python3 tools/agent_name_guard.py --message "$1"
